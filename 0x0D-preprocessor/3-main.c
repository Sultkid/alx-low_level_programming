#include <stdio.h>
#include "4-sum.h"
#include "3-function_like_macro.h"
int main (void)
{
	int r = ABS(-98) * 10;
	int x = ABS(98) * 10;
	int j = SUM(5, 10);
	printf("%d   %d   %d\n", r, x, j);
	return(0);
}
