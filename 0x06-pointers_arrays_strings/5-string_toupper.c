#include "main.h"

/**
 * string_toupper - main
 * @s: member
 * Return: s
 */
char *string_toupper(char *s)
{
	int i;

	for (i = 0; i < s[i]; i++)
	{
		if (s[i] >= 'a' || s[i] <= 'z')
		{
			char d = s[i] - 32;
		}
	}
	return (d);
}
